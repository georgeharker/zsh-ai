# Ties this repo to a crib memory project. `crib import` pulls the docs below
# into the `zsh-ai` project (one-way: source wins, ids/history preserved).
project: zsh-ai
import:
  - README.md
  - index.md
  - docs/**/*.md

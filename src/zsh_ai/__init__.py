"""zsh-ai — OpenAI-compatible LLM integration for zsh.

Three submodules, each with its own bin/ entry point:

  llm/     OpenAI-compatible bridge — bin/zsh-ai-llm
  render/  Markdown one-shot renderer — bin/mdrender
  view/    Modal scrolling viewer for streamed output — bin/mdview
"""
__version__ = "0.1.0"

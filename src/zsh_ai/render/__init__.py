"""Markdown rendering: one-shot CLI (``mdrender``) and the incremental
``LiveMarkdownStream`` used by the rendered-output path.

Entry: ``python -m zsh_ai.render`` → :mod:`.__main__` → :func:`.cli.main`.
"""

#!/usr/bin/env zsh
# zsh-llm — CLI launcher. Sources the plugin and dispatches to its function.

emulate -L zsh

# Resolve plugin root from this script's location: <root>/bin/zsh-llm
SCRIPT_DIR="${0:A:h}"
PLUGIN_ROOT="${SCRIPT_DIR:h}"

source "$PLUGIN_ROOT/zsh-llm.plugin.zsh"
zsh-llm "$@"

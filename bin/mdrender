#!/usr/bin/env zsh
# bin/mdrender — pipe stdin through the rich markdown renderer.
#
# Useful headlessly:
#   cat sample.md           | ./bin/mdrender
#   ./bin/zsh-ai-llm chat … | ./bin/mdrender --stream
#   ./bin/mdrender --pager < sample.md
#
# Setup: `uv sync` in the plugin directory.

exec "${0:A:h:h}/.venv/bin/python" -m zsh_ai.render "$@"
